# MVP 执行计划

## 状态

- 状态：Active
- 建立日期：2026-09-21
- 当前阶段：M3 PowerPoint COM 真实预览链路已通过三页示例验收；九布局与版本绑定留待后续任务
- 产品范围：[../../PRODUCT.md](../../PRODUCT.md)
- 架构约束：[../../ARCHITECTURE.md](../../ARCHITECTURE.md)
- 测试策略：[../../TESTING.md](../../TESTING.md)

## 目标

在 Windows 上交付一个可直接运行的单机 Demo：用户通过自然语言和可选素材生成可编辑 PPTX，确认大纲后查看最终文件真实预览，并通过自然语言进行局部修改、保存版本和回退。

## 执行规则

- 按任务依赖顺序推进，不跨过前置门禁。
- 每项任务必须可以独立验证，并在合并前运行其“测试”条目。
- 测试默认使用 fake model provider，不依赖网络或真实密钥。
- 每完成一项，将复选框改为 `[x]`，并在“进展记录”写日期和结果。
- 新发现的范围、架构或测试变化先更新对应文档，再调整本计划。
- 本计划不要求一次提交完成一个里程碑；优先保持变更小而可回滚。

## MVP 退出条件

- [ ] 一句自然语言可生成 6～10 页可编辑 PPTX。
- [ ] 支持规定的文件类型并保留来源 ID。
- [ ] 用户可在生成前确认大纲。
- [ ] 预览来自当前 PPTX 的实际渲染。
- [ ] 指定页面、主题和顺序可通过自然语言局部修改。
- [ ] 每次成功变更生成不可变版本，失败不覆盖旧版本。
- [ ] PowerPoint 和 WPS 手工兼容检查通过。
- [ ] [TESTING.md](../../TESTING.md) 中的 MVP 自动门禁通过。

---

## M0：工程骨架与本地环境

### M0.1 初始化 Python 项目

- [x] 使用 `uv init --python 3.12` 创建项目元数据。
- [x] 设置应用包目录、测试目录和最小入口占位，不实现业务行为。
- [x] 将 `uv.lock` 纳入版本管理。

产物：`pyproject.toml`、`uv.lock`、包/测试目录。

测试：

```powershell
uv sync
uv run python --version
```

完成条件：命令退出码为 0，Python 主版本为 3.12，干净环境可重复同步。

依赖：无。

### M0.2 固定基础依赖与工具配置

- [ ] 添加运行依赖：Streamlit、OpenAI SDK、Pydantic、MarkItDown、python-pptx、Pillow、python-dotenv、httpx，以及 Windows 限定的 pywin32。
- [x] 添加开发依赖：pytest、Ruff。
- [x] 配置 Ruff 和 pytest markers（`integration`、`powerpoint`、`provider`、`e2e`）。

产物：更新后的 `pyproject.toml` 和 `uv.lock`。

测试：

```powershell
uv run python -c "import streamlit, pptx, pydantic, win32com.client"
uv run ruff check .
uv run pytest --collect-only
```

完成条件：依赖可导入，静态检查通过，pytest 能收集测试。

依赖：M0.1。

### M0.3 配置与启动前检查

当前阶段进展：

- [x] 增加 PowerPoint 预览后端、超时和 PNG 尺寸配置，并提供无密钥 `.env.example`。
- [x] 预览入口验证 Windows、PPTX 文件和 PowerPoint COM 能力，错误包含具体失败阶段。
- [ ] workspace 根目录及其他启动配置留在后续任务。

- [ ] 定义配置项及默认值，不包含真实密钥。
- [x] 添加 `.env.example`。
- [ ] 启动时检查 workspace 根目录和 PowerPoint COM 能力。
- [ ] 错误信息包含明确的 Windows 修复方法。

产物：配置模块、示例环境文件、配置测试。

测试：

- 无 `.env` 时只报告缺少的必需配置。
- 临时环境变量能覆盖默认值。
- 非 Windows、非法尺寸或不可用 PowerPoint 返回结构化配置错误。

完成条件：三种场景的单元测试通过，测试日志不包含密钥值。

依赖：M0.2。

### M0.4 工作区安全边界

- [ ] 实现项目 ID 生成和 workspace 路径解析。
- [ ] 拒绝绝对路径、`..`、符号链接逃逸和 Windows 保留设备名。
- [ ] 为项目创建隔离目录结构。

产物：storage 路径模块及安全测试。

测试：

- 合法项目 ID 解析到临时 workspace 内。
- 恶意路径输入全部被拒绝。
- 两个项目不能解析到同一目录。

完成条件：路径安全参数化测试通过，不对测试临时目录外产生写入。

依赖：M0.1。

### M0 里程碑门禁

- [ ] `uv sync`、Ruff 和测试收集通过。
- [ ] 配置与路径安全测试通过。
- [ ] README 中的实际命令与项目一致。

---

## M1：领域合同与版本存储

### M1.1 定义 ThemeSpec、SourceRef 和 AssetRef

- [x] 定义颜色、字体和主题字段约束。
- [x] 定义来源/资源 ID、显示名、类型和安全相对路径。
- [x] 禁止领域模型接受绝对本地路径。

测试：合法模型可序列化往返；非法颜色、重复 ID 和绝对路径被拒绝。

完成条件：单元测试覆盖有效、无效和边界输入。

依赖：M0.3、M0.4。

### M1.2 定义 SlideSpec 和九种布局合同

第一阶段进展：

- [x] 定义稳定 `slide_id`，并实现 `cover`、`bullets`、`closing` 三种布局。
- [x] 为三种布局定义必需字段、字段长度和容量上限。
- [ ] 实现其余六种布局后完成本任务。

- [ ] 定义页面稳定 ID 和支持的布局枚举。
- [ ] 为每种布局定义必需字段和容量上限。
- [ ] 对不适用于当前布局的冲突字段给出明确行为。

测试：每种布局至少有一个合法 fixture 和两个非法 fixture。

完成条件：九种布局的参数化 schema 测试通过。

依赖：M1.1。

### M1.3 定义 DeckSpec

第一阶段进展：

- [x] 定义 schema version、文稿元数据、主题、来源、资源和有序页面。
- [x] 校验全部 ID 唯一、页面引用存在、页面数量在 1～30 之间。
- [x] 添加 JSON fixture、序列化往返和非法输入测试。
- [ ] 九种布局全部完成后关闭本任务。

- [ ] 定义 deck 元数据、schema version、主题、来源、资源和有序页面。
- [ ] 校验所有 ID 唯一且引用存在。
- [ ] 保证显示顺序与稳定 ID 分离。

测试：规范 DeckSpec 可 JSON 往返；重复或悬空 ID 被拒绝；页面重排不改变 ID。

完成条件：领域不变量测试通过。

依赖：M1.2。

### M1.4 定义 PatchPlan

- [ ] 定义五种允许操作。
- [ ] 定义每种操作允许字段和目标约束。
- [ ] 拒绝未知操作、未知字段和不存在目标。

测试：每种操作的成功和失败 fixture；批量操作中任一失败时整体不应用。

完成条件：PatchPlan schema 和原子性前置测试通过。

依赖：M1.3。

### M1.5 实现不可变版本存储

- [ ] 保存 `deck.json`、状态和元数据。
- [ ] 使用临时文件 + 原子替换写入。
- [ ] 只允许 current 指向成功版本。
- [ ] 提供列出、读取和选择历史版本的接口。

测试：连续保存、写入中断、重复版本、损坏 JSON 和 current 回退场景。

完成条件：失败写入不改变 current，旧版本内容哈希不变。

依赖：M0.4、M1.3。

### M1 里程碑门禁

- [ ] 所有领域合同和不变量有单元测试。
- [ ] 可在临时目录保存、读取和切换版本。
- [ ] 无模型、UI 或 PPTX 依赖进入领域测试。

---

## M2：确定性 PPTX 渲染

### M2.1 建立渲染器骨架

第一阶段进展：

- [x] 将当前三布局 DeckSpec 渲染为 16:9 Presentation。
- [x] 统一主题颜色、字体、安全边距和页面尺寸。
- [x] 保存后用 python-pptx 重新打开并核对页数。
- [ ] 后续按完整 M2 合同补充结构化渲染结果和全部布局。

- [ ] 将 DeckSpec 转成 16:9 Presentation。
- [ ] 应用默认主题、母版级边距和字体规则。
- [ ] 保存文件并返回结构化渲染结果。

测试：最小 DeckSpec 生成非空 PPTX，可由 `python-pptx` 再次打开，页数正确。

完成条件：本地 smoke fixture 通过。

依赖：M1.3。

### M2.2 建立共享布局原语

- [ ] 实现标题、正文、页码、来源脚注、色块、图片裁剪等共享函数。
- [ ] 统一使用主题、网格和安全边距。
- [ ] 定义可测试的 shape 结构摘要。

测试：坐标在画布范围内；图片保持比例；主题值正确应用。

完成条件：共享原语单元测试通过。

依赖：M2.1。

### M2.3 实现 cover、section、closing

- [ ] 分别实现三个低密度布局。
- [ ] 对缺失可选字段使用确定性降级。

测试：每种布局的最小、上限和中文 fixture 均可生成并重新打开。

完成条件：结构摘要与批准 fixture 一致。

依赖：M2.2。

### M2.4 实现 bullets、two_column、image_text

- [ ] 实现文字容量限制和换行策略。
- [ ] 实现缺图时的无图降级。
- [ ] 图片通过 AssetRef 获取，禁止直接路径。

测试：接近容量、超量、缺图、横图和竖图 fixture。

完成条件：合法输入生成；超量输入产生明确 QA/验证错误而非不可读页面。

依赖：M2.2、M1.1。

### M2.5 实现 metrics、timeline、chart

- [ ] 支持 2～4 个指标卡。
- [ ] 支持 3～6 个时间轴节点。
- [ ] 支持柱、线、饼图的最小数据合同。

测试：合法上下限、空数据、负值、长标签和中文标签。

完成条件：图表/形状为可编辑对象，空或非法数据被拒绝。

依赖：M2.2。

### M2.6 添加渲染完整性检查

- [ ] 检查输出存在、非空、ZIP 可读、可重新加载。
- [ ] 比较 DeckSpec 页数与 PPTX 页数。
- [ ] 生成用于测试的规范化 shape 摘要。

测试：正常文件、截断文件、错误页数和全页图片异常 fixture。

完成条件：损坏或不合格文件不会标记为成功。

依赖：M2.3～M2.5。

### M2 里程碑门禁

- [ ] 九种布局全部具有组件测试。
- [ ] 一个包含九种布局的 deck 可成功生成和重新打开。
- [ ] 输出包含原生可编辑对象，不是全页截图集合。

---

## M3：PPTX 真实预览

### M3.1 检测 PowerPoint COM 能力

- [x] 从配置读取后端、超时和 PNG 尺寸。
- [x] 验证当前系统为 Windows，并由独立工作进程调用 `DispatchEx("PowerPoint.Application")`。
- [x] 未安装 PowerPoint 或 COM 启动失败时抛出包含原因的 `PreviewError`。

测试：非 Windows、未安装 PowerPoint、COM 启动失败和模拟超时。

完成条件：Windows 路径测试通过。

依赖：M0.3。

### M3.2 实现 PPTX → PowerPoint COM → PNG

- [x] 使用参数数组启动独立 Python 子进程，不拼接 shell 命令。
- [x] COM 工作线程执行 `CoInitialize`、只读打开、逐页 `Slide.Export` 和 `finally` 清理。
- [x] 主进程设置超时、捕获 stdout/stderr，并在超时时清理本次 PowerPoint 进程。

测试：Mock COM 验证生命周期；损坏文件、打开失败、导出失败和超时返回结构化错误。

完成条件：`integration and powerpoint` marker 测试在安装环境通过。

依赖：M2.6、M3.1。

### M3.3 实现 PNG 产物提交

- [x] 先导出到独立临时目录，全部成功后移动到唯一成功目录。
- [x] 固定 `slide_NNN.png` 命名顺序，默认尺寸为 1920×1080。
- [x] 返回页面路径、数量和尺寸元数据。

测试：PNG 数量、尺寸、顺序正确；失败不覆盖已有成功预览。

完成条件：普通测试完全 Mock 外部进程，不启动 PowerPoint。

依赖：M0.2。

### M3.4 实现预览一致性门禁

当前阶段进展：

- [x] 比较 PPTX 和 PNG 页数并校验全部 PNG 尺寸。
- [x] 每次使用独立输出目录，失败时不会返回或复用旧预览。
- [ ] DeckSpec 版本、PPTX 哈希和预览绑定留在后续任务。

- [ ] 比较 DeckSpec、PPTX 和 PNG 页数。
- [ ] 将预览与 DeckSpec 版本/PPTX 哈希绑定。
- [x] 转换失败时不复用其他版本的旧预览。

测试：页数不一致、过期预览和转换失败场景。

完成条件：只有完全匹配的预览能标记为当前版本。

依赖：M3.2、M3.3。

### M3 里程碑门禁

- [ ] 九布局测试 deck 可生成全部 PNG。
- [ ] 预览元数据绑定正确版本。
- [x] PowerPoint 缺失或 COM 失败时错误清晰且不伪造成功。

---

## M4：素材接入

### M4.1 安全保存上传文件

- [ ] 校验扩展名、大小和允许的 MIME。
- [ ] 磁盘文件名使用生成 ID，保留原始显示名元数据。
- [ ] 同名上传互不覆盖。

测试：合法文件、超大文件、双扩展名、路径穿越和同名文件。

完成条件：所有写入限定在当前项目 uploads 目录。

依赖：M0.4、M1.1。

### M4.2 解析文本型材料

- [ ] 用 MarkItDown 处理 PDF、DOCX、PPTX、XLSX、Markdown 和文本。
- [ ] 将解析结果保存到 materials 目录。
- [ ] 保留 source ID、文件名和可用定位信息。

测试：每种格式一个小 fixture；空/损坏文件产生明确错误。

完成条件：支持格式输出非空规范 Markdown，错误不影响其他文件。

依赖：M4.1。

### M4.3 建立材料截断与分块

- [ ] 按标题、页或段落切块。
- [ ] 设置总字符预算并记录被截断状态。
- [ ] 保留块到 source ID/locator 的映射。

测试：短文、超长文、多来源和中文边界。

完成条件：相同输入产生稳定块顺序，预算不会被突破。

依赖：M4.2。

### M4.4 建立图片资源注册表

- [ ] 读取尺寸、格式和方向。
- [ ] 注册 asset ID 和安全相对路径。
- [ ] 拒绝损坏或不支持图片。

测试：PNG/JPEG、横图/竖图、损坏图片和伪造扩展名。

完成条件：渲染器只能通过 AssetRef 使用上传图片。

依赖：M4.1、M1.1。

### M4 里程碑门禁

- [ ] 所有 MVP 格式有解析 fixture。
- [ ] 来源与资源 ID 可进入 DeckSpec 并被验证。
- [ ] 用户文件错误不会破坏项目状态。

---

## M5：模型规划与结构化输出

### M5.1 定义 ModelProvider 接口和 FakeProvider

- [ ] 定义生成 DeckSpec/PatchPlan 的领域接口。
- [ ] 实现确定性 FakeProvider。
- [ ] 定义超时、拒绝、无效结构和提供商错误类型。

测试：fake 成功和所有错误分支；断言领域层没有 SDK 类型。

完成条件：后续集成测试无需网络或密钥。

依赖：M1.3、M1.4。

### M5.2 实现首个真实提供商适配器

- [ ] 从配置创建客户端。
- [ ] 使用结构化输出生成 Pydantic 模型。
- [ ] 设置超时和有限重试。
- [ ] 不记录密钥或完整敏感材料。

测试：mock SDK 响应；可选真实 provider 契约测试。

完成条件：适配器通过 mock 测试，真实测试默认跳过且可显式启用。

依赖：M0.3、M5.1。

### M5.3 实现 DeckSpec 规划器

- [ ] 组合用户要求、材料块、布局目录和产品约束。
- [ ] 对模型结果执行完整 DeckSpec 校验。
- [ ] 在 UI 使用前返回大纲视图模型。

测试：无材料、单材料、多材料、超长材料和非法模型输出。

完成条件：FakeProvider 集成测试生成有效 DeckSpec，来源引用均存在。

依赖：M4.3、M5.1。

### M5.4 实现规划失败与降级

- [ ] 区分可重试与不可重试错误。
- [ ] 模型输出无效时最多执行有限修复请求或直接失败。
- [ ] 不保存无效候选版本。

测试：超时、拒绝、非法 JSON、非法布局、悬空来源和重试耗尽。

完成条件：错误可操作，项目仍保持最后成功状态。

依赖：M5.2、M5.3。

### M5 里程碑门禁

- [ ] FakeProvider 完成“请求 → DeckSpec → PPTX”的集成链路。
- [ ] 可选真实提供商能生成最小合法 DeckSpec。
- [ ] 所有错误路径不写入成功版本。

---

## M6：局部修改、版本与回退

### M6.1 实现 PatchPlan 生成器

- [ ] 将用户指令、当前 DeckSpec 和允许操作交给 ModelProvider。
- [ ] 只返回结构化 PatchPlan。
- [ ] 对“第 N 页”先映射到当前 slide ID，再生成/验证操作。

测试：页面内容、主题、插入、删除、移动五类请求；模糊目标返回澄清错误。

完成条件：FakeProvider 场景全部生成合法目标 ID。

依赖：M5.1、M1.4。

### M6.2 实现 Patch 原子应用

- [ ] 在深拷贝上按顺序应用操作。
- [ ] 限制 payload 可修改字段。
- [ ] 完成后重新校验完整 DeckSpec。

测试：五类操作、批量成功、批量中途失败、未知字段和不存在目标。

完成条件：失败前后原 DeckSpec 序列化结果完全相同。

依赖：M1.4。

### M6.3 生成新版本并保留旧产物

- [ ] Patch 成功后生成候选版本。
- [ ] 通过渲染、预览和 QA 后提交 current。
- [ ] 失败候选保留诊断或安全清理，不覆盖旧产物。

测试：成功 v002、渲染失败、预览失败和 QA fail。

完成条件：current 只在全部门禁通过后移动。

依赖：M1.5、M2.6、M3.4、M6.2。

### M6.4 实现回退

- [ ] 列出成功版本。
- [ ] 选择历史版本并恢复为当前可见状态。
- [ ] 明确回退是否创建新版本，并按架构决策固定行为。

测试：回退 v001、回退不存在版本、回退损坏版本。

完成条件：回退后 DeckSpec、PPTX 和预览均对应同一目标内容。

依赖：M6.3。

### M6 里程碑门禁

- [ ] “改第3页、换主题、插入一页、移动一页、删除一页”全部通过集成测试。
- [ ] 未命中页面的规范化 DeckSpec 摘要不变。
- [ ] 回退不丢失任何成功历史版本。

---

## M7：Streamlit 用户闭环

### M7.1 项目和会话 UI

- [ ] 新建/选择项目。
- [ ] 在 session state 中只保存轻量 ID 和视图状态。
- [ ] 从 storage 重新加载权威状态。

测试：新会话、刷新、两个项目切换和无效项目 ID。

完成条件：刷新页面不丢失已提交项目版本。

依赖：M1.5。

### M7.2 需求输入与素材上传 UI

- [ ] 提供聊天输入、页数、主题、语言和文件上传。
- [ ] 显示每个文件解析成功/失败状态。
- [ ] 阻止无效或超限上传进入规划流程。

测试：Streamlit AppTest 或等价测试覆盖空请求、有效上传和错误上传。

完成条件：只有有效请求和材料可进入大纲生成。

依赖：M4、M7.1。

### M7.3 大纲确认 UI

- [ ] 显示页面顺序、标题、布局和一句话摘要。
- [ ] 支持确认和重新规划。
- [ ] 确认前不生成最终 PPTX。

测试：首次大纲、重新规划、确认和模型失败。

完成条件：工作流状态只允许合法转换。

依赖：M5.3、M7.2。

### M7.4 生成、预览和下载 UI

- [ ] 显示生成进度和结构化错误。
- [ ] 展示当前版本 PNG 网格。
- [ ] 仅在 QA 通过时提供 PPTX 下载。

测试：成功生成、渲染失败、预览失败、QA fail 和下载文件名。

完成条件：界面不会展示或下载错误版本产物。

依赖：M2、M3、M7.3。

### M7.5 多轮修改和回退 UI

- [ ] 已有 DeckSpec 时聊天输入进入 Patch 流程。
- [ ] 显示 Patch 摘要和新版本。
- [ ] 提供版本列表与回退操作。

测试：指定页面修改、主题修改、失败修改和回退。

完成条件：端到端测试完成至少一次修改和回退。

依赖：M6、M7.4。

### M7 里程碑门禁

- [ ] FakeProvider 下完整用户旅程自动通过。
- [ ] UI 刷新后状态可恢复。
- [ ] 任何失败不提供错误版本下载。

---

## M8：确定性 QA 与下载门禁

### M8.1 内容容量检查

- [ ] 标题、要点、左右栏、指标和时间轴容量规则。
- [ ] 空页面、重复标题和必需字段检查。

测试：每条规则有 pass/warn/fail fixture。

完成条件：QAReport issue code 稳定且可定位 slide ID。

依赖：M1.2。

### M8.2 引用与资源检查

- [ ] 检查 source/asset 引用存在。
- [ ] 检查文件存在、图片可读和资源类型匹配。

测试：悬空引用、缺失文件、损坏图片和正确资源。

完成条件：任何 fail 都阻止候选版本提交。

依赖：M4.4、M1.3。

### M8.3 产物一致性检查

- [ ] 检查 PPTX 完整性和页数。
- [ ] 检查 PNG 页数与版本绑定。
- [ ] 记录 PPTX 哈希到 QAReport。

测试：过期预览、错误页数、损坏 PPTX 和正确产物。

完成条件：下载端点/按钮只接受通过 QA 的当前版本。

依赖：M2.6、M3.4。

### M8.4 QA 报告 UI

- [ ] 显示 fail/warn 及对应页面。
- [ ] 对可修复问题给出操作建议。
- [ ] 不把 warning 伪装成成功无问题。

测试：无问题、只有 warning、存在 fail 三种 UI 状态。

完成条件：用户可理解为什么文件不能下载。

依赖：M8.1～M8.3、M7.4。

### M8 里程碑门禁

- [ ] 所有确定性 QA 规则有测试。
- [ ] fail 级问题阻止提交和下载。
- [ ] warning 被记录但按产品规则决定是否允许下载。

---

## M9：端到端验收与交付

### M9.1 建立三套验收场景

- [ ] 无材料：研究生开题汇报。
- [ ] 文档材料：从 PDF/DOCX 生成业务汇报。
- [ ] 数据材料：从 XLSX 生成带基础图表的经营汇报。

每套场景包含初始请求、FakeProvider fixture、修改请求和期望结构摘要。

测试：三套场景均完成创建、预览、修改、下载和回退。

完成条件：E2E 测试稳定重复通过。

依赖：M7、M8。

### M9.2 Windows 冷启动验证

- [ ] 在没有既有虚拟环境的 Windows 环境执行 README 安装步骤。
- [ ] 验证包含空格和中文的仓库路径。
- [ ] 验证 PowerPoint COM 可启动、超时可恢复且预览目录可写。

测试：记录命令、耗时、发现的问题和修正。

完成条件：按 README 可从干净环境启动 Demo。

依赖：全部实现里程碑。

### M9.3 Office 兼容性检查

- [ ] 用 Microsoft PowerPoint 打开并编辑验收文件。
- [ ] 用 WPS 打开并编辑验收文件。

测试：按 [TESTING.md](../../TESTING.md) 手工清单记录结果。

完成条件：无修复提示，核心对象可编辑，无 P0/P1/P2 未处理问题。

依赖：M9.1。

### M9.4 文档与发布门禁

- [ ] README 快速开始与实际命令一致。
- [ ] `.env.example` 完整且无真实密钥。
- [ ] 产品、架构、测试文档反映最终 MVP。
- [ ] 归档本计划或创建后续 active 计划。

测试：执行全部静态检查、非 provider 测试和文档链接检查。

完成条件：MVP 退出条件全部勾选。

依赖：M9.2、M9.3。

---

## 进展记录

| 日期 | 任务 | 结果 |
|---|---|---|
| 2026-09-21 | 项目初始化规划 | 创建 AGENTS、README、产品/架构/测试文档和可独立测试的 MVP 任务；未创建业务代码 |
| 2026-09-21 | M0.1 与第一阶段工具初始化 | 使用 uv 固定 Python 3.12，创建最小 src/test 骨架、环境测试和 `.env.example`；仅安装 Pydantic、pydantic-settings、python-pptx、pytest、Ruff，未开始业务实现 |
| 2026-09-21 | DeckSpec 数据合同第一阶段 | 实现 SourceRef、AssetRef、ThemeSpec、SlideSpec、DeckSpec；仅开放 cover、bullets、closing，加入 JSON 往返、ID、布局、颜色、容量、引用和路径测试；未实现 PatchPlan 或渲染 |
| 2026-09-21 | 确定性 PPTX 渲染第一阶段 | 使用原生可编辑文本框和形状实现 cover、bullets、closing；固定尺寸、主题与边距，保存后重新打开校验，并提供 sample_deck 生成脚本；未实现图片、图表或其他布局 |
| 2026-09-22 | PowerPoint 真实预览链路 | 将未提交的旧预览方案替换为 PowerPoint COM 逐页 PNG；31 个普通测试、1 个真实集成测试及 Ruff 通过，三页 1920×1080 PNG 目视检查正常，完成后无残留 PowerPoint 进程 |

## 决策记录

| 日期 | 决策 | 原因 |
|---|---|---|
| 2026-09-21 | MVP 使用 Windows 原生 Python 单体 | 最快形成可运行闭环，减少前后端和基础设施成本 |
| 2026-09-21 | DeckSpec 为唯一事实源 | 支持确定性渲染、多轮局部修改、版本和回退 |
| 2026-09-21 | 最终预览必须来自 PPTX | 避免网页预览与下载文件不一致 |
| 2026-09-21 | 自动测试默认使用 FakeProvider | 消除网络、成本、随机性和密钥依赖 |
| 2026-09-22 | 真实预览使用 PowerPoint COM 独立子进程 | 以目标 Office 渲染器保证预览一致性，并用进程隔离、超时和临时目录控制卡死与半成品风险 |

## 已知风险

| 风险 | 当前应对 |
|---|---|
| python-pptx 对复杂 PowerPoint 特性覆盖有限 | MVP 限制布局和对象类型；后续可替换 PptxGenJS/Open XML SDK |
| PowerPoint COM 依赖桌面 Office、用户会话与许可状态 | 启动时分类报告 COM 错误；用独立子进程隔离卡死，并保留上一成功预览 |
| 中文字体在不同机器上不一致 | 启动检查字体，定义后备字体并在验收环境验证 |
| 模型结构化输出能力因提供商而异 | 使用适配器、Pydantic 校验、FakeProvider 和提供商契约测试 |
| Office/PDF 解析结果可能丢失布局语义 | MVP 以内容提取为主，保留来源定位；不承诺无损导入 |
| 长材料可能超出上下文 | MVP 使用稳定分块和字符预算，后续再引入检索 |

## 待决问题

- 回退应移动 current 指针，还是创建一个内容等同的新版本？在 M6.4 开始前决定。
- warning 级 QA 是否允许下载？在 M8.3 开始前按具体 issue code 决定。
- 首个受支持的真实模型及最小能力基线是什么？在 M5.2 开始前通过当前官方文档和账户能力确认。
- PowerPoint 手工兼容测试采用哪些具体版本？在 M9.3 开始前记录环境。
